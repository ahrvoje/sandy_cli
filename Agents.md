Before modifying Sandy source, read .agents/rules/prereq.md and follow all referenced .agents documents.

Treat .agents/guidelines.md, .agents/acl_design.md, and .agents/resilience.md as project policy, follow it as specified and give opinions on design improvements.

Use only the build command documented in .agents/guidelines.md.

Do not use fallback build commands unless the documented one fails and the user approves deviation.
