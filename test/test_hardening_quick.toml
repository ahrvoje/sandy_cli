# Sandy hardening test — minimal config for quick log marker tests.
# Uses cmd.exe (instant startup) instead of Python.

[sandbox]
token = 'restricted'
integrity = 'low'
workdir = 'C:\Windows\System32'

[allow]
execute = ['C:\Windows\System32']
[environment]
inherit = true

[limit]
timeout   = 10
