# Test AC allow-only: grant READ to test_RW
# Expected: can read, cannot write, cannot delete (no deny — just limited grant)
# For actual deny testing, use the RT variant (test_deny_restricted_config.toml)
[sandbox]
token = 'appcontainer'

[allow.deep]
execute = ['C:\Users\H\AppData\Local\Programs\Python\Python314']
read = ['C:\Users\H\test_RW']

[environment]
inherit = true

[limit]
timeout = 30
