# Sandy resilience test — restricted token config for dry-run / print-config tests.
# Uses a known absolute workdir to verify output correctness.

[sandbox]
token = 'restricted'
integrity = 'low'
workdir = 'C:\Users\H'

[allow]
read    = ['C:\Users\H']
[environment]
inherit = true
