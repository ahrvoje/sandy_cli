# Sandy profile test configuration — Restricted Token
# Minimal config for profile creation/run tests

[sandbox]
token = 'restricted'
integrity = 'low'

[allow]
read    = ['C:\Users\H\test_sandy_profile\data']
[environment]
inherit = true

[limit]
timeout   = 30
