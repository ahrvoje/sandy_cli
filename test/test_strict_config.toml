# Sandy test configuration — strict mode test
# system_dirs is not set (strict), so no executable should be able to run.

[sandbox]
token = 'appcontainer'

[access]
read = [
    'C:\Users\H\AppData\Local\Programs\Python\Python314',
    'C:\repos\sandy_cli\',
]

[allow]
system_dirs = false  # strict isolation — no system dir access
network = false
localhost = false
lan = false
stdin = false
clipboard_read = false
clipboard_write = false
child_processes = true

[environment]
inherit = true
