# Adversarial "Break Sandy" test configuration (AC, no deny)
#
# TREE:
#   test_evil/
#     scripts/              [allow read]  — test script
#       passage/            [allow all]   — contains junction escape
#       vault/              (no grant)    — primary target
#       LongSecretName/     (no grant)    — 8.3 alias target
#       records/            [allow read]  — ADS + write bypass (read-only)
#       CaseSensitive/      (no grant)    — case mismatch target
#       deep/               [allow all]   — MAX_PATH stress

[sandbox]
token = 'appcontainer'

[allow.deep]
execute = ['C:\Users\H\AppData\Local\Programs\Python\Python314']
read = ['C:\Users\H\test_evil\scripts', 'C:\Users\H\test_evil\arena\records']
all = ['C:\Users\H\test_evil\arena\passage', 'C:\Users\H\test_evil\arena\deep']

[environment]
inherit = true

[limit]
timeout = 60
