# Sandy — AppContainer Config Template
# ============================================================
# AppContainer is the default sandbox mode. It provides kernel-enforced
# isolation with network control and object namespace isolation.
# Named pipe creation is blocked (kernel restriction).
#
# Only [sandbox] token is required. All other sections are optional
# with safe defaults (deny-all / disabled / unlimited).

[sandbox]
token = 'appcontainer'
# workdir = 'inherit'      # optional (default: exe folder)

# ============================================================
# ACCESS — File and folder grants (optional, default: no grants)
# ============================================================

# [allow.deep]
# peek    = []              # non-recursive: list dir + stat only
# read    = []              # read files, list dirs (recursive)
# write   = []              # create/modify files (recursive)
# execute = []              # read + execute (recursive)
# append  = []              # append only (recursive)
# delete  = []              # delete only (recursive)
# all     = []              # full access (recursive)

# [deny.deep] is NOT available in appcontainer mode (kernel ignores deny ACEs for AC SIDs)

# ============================================================
# PRIVILEGES — Optional, safe defaults shown
# ============================================================

# [privileges]
# network         = false   # default false: no outbound internet
# localhost       = false   # default false: no loopback (admin required)
# lan             = false   # default false: no local network
# stdin           = false   # default false (NUL). true=inherit, path=file
# clipboard_read  = false   # default false
# clipboard_write = false   # default false
# child_processes = true    # default true

# NOTE: named_pipes, desktop are not available in AppContainer mode.
# NOTE: [registry] section is not available in AppContainer mode.

# ============================================================
# ENVIRONMENT — Optional, default: filtered
# ============================================================

# [environment]
# inherit = false            # default false (clean environment)
# pass = []                  # default [] (pass no extra variables)

# ============================================================
# RESOURCE LIMITS — Optional, default: unlimited (0)
# ============================================================

# [limit]
# timeout = 0               # 0 = no timeout
# memory = 0                # 0 = no cap (MB)
# processes = 0             # 0 = no cap
